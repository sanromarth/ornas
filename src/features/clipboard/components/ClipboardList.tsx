/** ClipboardList — virtualized scrollable list of clipboard items. */


/** Renders the main virtualized list of clipboard entries. */
export function ClipboardList() {
  return <div data-testid="clipboard-list" className="flex-1 overflow-hidden" />;
}
