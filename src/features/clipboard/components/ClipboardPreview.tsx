/** ClipboardPreview — detail view for the selected clipboard item. */


/** Renders the full content preview of the currently selected clip. */
export function ClipboardPreview() {
  return <div data-testid="clipboard-preview" className="flex-1 overflow-auto" />;
}
