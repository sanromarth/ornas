//! Settings service — manages application configuration with defaults and validation.

/// Settings service that handles configuration with typed defaults.
pub struct SettingsService;

impl SettingsService {
    pub fn new() -> Self {
        Self
    }
}
