//! ORNAS desktop application entry point.
//!
//! This file simply calls the library's `run()` function.
//! All application logic lives in `lib.rs` and its sub-modules.

fn main() {
    ornas_lib::run();
}
